[package]
name = "mr60bha2-proto"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Seeed MR60BHA2 60 GHz mmWave radar sensor protocol parser"
readme = "README.md"
keywords = ["radar", "mr60bha2", "mmwave", "uart", "no-std"]
categories = ["embedded", "hardware-support", "parser-implementations", "no-std"]

[dependencies]
libm = "0.2"

[features]
default = ["std"]
std = []
